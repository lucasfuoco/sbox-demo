namespace Sandbox;

/// <summary>
/// Preset weather behaviour for a localized volume.
/// Use <see cref="WeatherVolume"/> overrides to tune individual samples.
/// </summary>
public enum WeatherVolumeType
{
	RainCloud,
	StormCloud,
	FogBank,
	SnowCloud,
	DustStorm,
	ToxicGas,
	AshCloud,
	ClearCloud,
}
